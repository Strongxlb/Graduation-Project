# 待导师确认事项 - Open Questions

> 配套文件：[`../README.md`](../README.md)、[`../plan1.md`](../plan1.md)
> 维护规则：
> - 进会议前先看一遍本表，挑出本周需要导师拍板的项目。
> - 每条都要带「我的初步建议」，不要只带问题。
> - 状态字段：`open`（待讨论）、`pending`（导师承诺回复中）、`decided`（已定）。
> - 决议后：把结论写进 `decided` 行，并在 README / plan 中同步引用。

---

## 1. 当前未决事项（按优先级排序）

| # | 问题 | 我的初步建议 | 决策影响 | 优先级 | 状态 |
| - | --- | --- | --- | --- | --- |
| Q1 | 真实管网数据是否可获取？是否有合作水司提供 `.inp` + 余氯监测数据？ | 若 4 周内拿不到真实数据，使用 WNTR 自带 `Net1.inp` 跑通流程，Week 3 切到 BWSN benchmark 或 `Net3.inp` 作为论文主案例。 | 决定 T2 / T3 是否需要数据清洗周；影响 Methodology 写法。 | P0 | open |
| Q2 | 余氯阈值是否锁定 `0.2 mg/L`？还是英国 / Imperial 内部有别的推荐值？ | 论文主图采用 `0.2 mg/L` 占位，并对 `0.1 / 0.3 mg/L` 做敏感性分析作为附录。 | 影响所有「阈值概率」结果图与 Discussion 的工程结论。 | P0 | open |
| Q3 | 测量误差分布形式：固定误差、相对误差、正态、截断分布？ | Week 4 之前先用 `obs = true + ε`，`ε ~ N(0, σ²)`，`σ = max(0.02 mg/L, 0.05·true)`；待文献 D2/D3 与导师反馈后再 refine。 | 决定 Monte Carlo 采样器与似然函数形式。 | P0 | open |
| Q4 | 校准方法路线：Plan A (MC + 最小二乘) vs Plan B (Bayesian / MCMC)？ | Week 6–7 先做 Plan A baseline；Week 8–9 视进度引入 Plan B（emcee 或 pymc）。论文写两路对照。 | 决定 Week 8–9 的工作量与是否需要 GPU / 大算力。 | P1 | open |
| Q5 | 论文使用的网络规模：`Net1` 太小，`Net3` 中等，BWSN 大？ | 主案例 `Net3`（约 90 节点），附录展示 `Net1` 用作教学示例。若数据允许，再补 BWSN。 | 影响计算成本与图表可读性。 | P1 | open |
| Q6 | AI 工具使用披露口径：是否在论文附录写明 prompt 与用途？ | 按 CEE / Imperial 「Use of generative AI tools」声明模板，加一节 Acknowledgement of AI use，列出：代码辅助、语言润色、图表说明。 | 论文格式与学术诚信风险。 | P1 | open |
| Q7 | 结果图是否需要双语（中英）？poster 是否中英双语？ | 论文与 poster 全英文；本仓库内部文档可保留中文。 | 影响图表导出量。 | P2 | open |
| Q8 | 是否需要做 sensor placement 优化（采样点设计）？ | 不作为主线；在 Discussion 里点一笔，未来工作展开。 | 影响 scope 是否需要扩张。 | P2 | open |

---

## 2. 已确认事项（决议归档）

> 当某条 Q 被讨论结束，把「问题 + 决议 + 决议日期」搬到这里，并在第 1 节中删除该行。

| # | 问题 | 决议 | 决议日期 | 备注 |
| - | --- | --- | --- | --- |
| _空_ | _等待第一次正式 weekly meeting_ | _-_ | _-_ | _-_ |

---

## 3. 给导师的问题清单（下次会议直接念）

这一节是把上表里 **本周必须问** 的几条抽出来，按 weekly meeting 的语序组织成可以直接读的版本。每次会议前更新。

> Meeting on: **TBD（建议 2026-05-22 或下一次固定时间）**

1. **关于数据**：我们是否有真实管网 + 余氯观测可用？如果短期内（4 周内）无法到位，我打算用 WNTR 自带 `Net1` 起步，Week 3 切到 `Net3` 或 BWSN benchmark 作为论文主案例。您觉得这个 fallback 是否可行？（对应 Q1, Q5）
2. **关于阈值**：论文里我打算把 `0.2 mg/L` 当成主阈值，并补 `0.1 / 0.3 mg/L` 的敏感性。您是否有更明确的推荐？（对应 Q2）
3. **关于误差模型**：在拿到真实数据之前，我计划先假设 `obs = true + N(0, σ²)`，`σ = max(0.02 mg/L, 5%·true)`。您是否同意先用这个占位假设？（对应 Q3）
4. **关于方法路线**：我打算 Week 6–7 先做 MC + 最小二乘的 baseline，Week 8–9 再加 Bayesian/MCMC。您倾向哪种作为论文「主结果」？（对应 Q4）
5. **关于 AI 披露**：我会在论文附录加 Acknowledgement of AI use，列出代码辅助 / 语言润色用途。您那边是否有特定模板要求？（对应 Q6）

---

## 4. 行动项 (Action items)

> 每次会议结束 5 分钟内填写，避免遗忘。

| # | 行动项 | 负责人 | 截止 | 状态 |
| - | --- | --- | --- | --- |
| _A0_ | 第一次正式 weekly meeting 排期 | 我 | 2026-05-22 前 | open |

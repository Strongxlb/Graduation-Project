# Meeting 2026-06-02（Tuesday · 首次正式 Supervisor Meeting）

> 汇报人：毕设学生
> 项目：Uncertainty-aware calibration of first-order chlorine decay — Bristol Water Field Lab（3-DMA）（CIVE70058）
> 仓库：https://github.com/Strongxlb/Graduation-Project.git
> 配套：[`../../plan1.md`](../../plan1.md)、[`../../README.md`](../../README.md)、[`open_questions.md`](open_questions.md)
> 准备日期：2026-06-01 ｜ 取代未召开的 [`2026-05-22.md`](2026-05-22.md)（原定 5/22 未开会，导师 **2026-05-25 邮件** 已锁定范围，本稿据此查漏补缺）

---

## 0. 30 秒电梯陈述（Opening）

5/22 未开会，但您 **5/25 邮件** 已锁定范围（3 个 DMA、10 个 chlorine monitors、first-order kinetics、ensemble-based 不确定性；排除 hydraulic calibration / MSX / 运营优化）。据此我已：① 读完您指定的 **A1/A3/A4/A6 + B1/B2**（"read the two references first" 即 B1/B2 已完成）；② 跑通您的 starter notebook，并在 Net3 上做了 **5 道自学练习**（含一个亲手做出的 **k_b–k_w 补偿沟**，提前验证 identifiability 问题）。今天希望拍板 **6 项决策**：数据交接、`.inp` 与管材信息、`k_b` 是否跨 DMA 共享、"ensemble-based" 的具体口径、WP1–WP5 结构、阈值与误差模型。

---

## 1. 自上次以来完成了什么？（Week 1 + Week 2）

### 1.1 项目骨架与工具链（T2 启动 ✅，承自 Week 1）

1. **仓库 + 环境就绪**：目录结构按 plan 建好；Conda `cive70058`（Python 3.11、WNTR 1.4、EPANET）可一键复现；`README` / `plan1` / `requirements` 齐备。
2. **版本管理**：Git/GitHub 流程跑顺；Week 2 产出（练习本、B1 笔记、literature 更新）均已 commit；本地领先 `origin/main`，待 push。
3. **Net1 demo**（baseline 参照）：`src/01_demo_wntr.py`（调试版）+ `src/02_demo_wntr_original.py`（官方 Net1，开 bulk+wall）已跑通并出图。

### 1.2 ⭐ 本周新增（Week 2 核心）

**(a) 读完导师指定文献（A1/A3/A4/A6 + B1/B2）并做笔记**

| 文献 | 关键 takeaway（已连到方法） |
| --- | --- |
| A1 Rossman 1994 | first-order bulk+wall+mass-transfer = 我要用的 EPANET 模型基础 |
| A6 Vasconcelos 1997 | 5 个真实系统验证；`k_b` 跨水源 200×、`k_w ∝ 1/roughness`；标定精度 **0.05–0.15 mg/L**（我的精度基线）；**烧瓶定 `k_b`、只标 `k_w`** |
| A4 Hallam 2002 | wall decay 随管材/管龄异质 → **支撑"每个 DMA 一个 `k_w`"** |
| A3 Powell 2000 | bulk decay 受 TOC/温度控制 → 用作 `k_b` 先验 |
| B1 Klise 2017 (WNTR) | EPANET 封装 + NetworkX + Pandas + **内建 Monte Carlo** = 我做 ensemble 的引擎（注：它是 forward-MC/resilience 论文，校准层我自己加）。已写精读笔记，并修正 literature.md 中 DOI（应为 `…06.022`） |
| B2 EPANET 2.2 Manual | 水质数值：bulk/wall/mass-transfer 项、单位约定（⚠️ 1/day ↔ 1/s） |

**(b) 跑通 starter notebook + 5 道 Net3 自学练习**（`learning/02_exercises.ipynb`，已纳入 git）

| 练习 | 实测结果 | 对项目的意义 |
| --- | --- | --- |
| 1 Bulk vs Wall | wall 占比最大 node 15（≈15%），近源点≈0 | 哪些 monitor 携带 `k_w` 信息 → 选点 |
| 2 Boundary scaling | 归一化曲线**完全叠合**（偏差≈0） | first-order 线性 → 入口实测可直接当边界 |
| 3 Mass transfer | D×0.5 → 最小压力 −1624 psi **水力崩溃**；D×0.8 也已负压 | 标定 `k_w` 绝不动管径；4/D 是 DMA 异质性根源 |
| 4 Time-varying inlet | `add_source`+`add_pattern` 注入日变化成功 | Bristol 入口边界的实现机制 |
| 5 Brute-force calib | (k_b,k_w) 网格 SSE 呈**对角"补偿沟"** | identifiability → 必须做不确定性 + 烧瓶定 `k_b` |

> **亮点**：练习 5 我已**亲手复现 `k_b–k_w` 补偿沟**——这正是后面 GLUE / Bayesian 的动机，且与 A6"固定 `k_b`"建议吻合。

### 1.3 文献与笔记状态（T1）

- `literature.md`：A–F 分组、CrossRef 验证、下载/阅读/理解三列；B1 标记已读 + 笔记链接。
- 精读笔记已入库：A1/A2/A3/A4/A6、B1（+ C1/C2/C5、D2、E1/E3/E5、F2）。
- 下一批方法论文待读：**B3（WNTR manual）、E6（GLUE）、E7（Gelman Ch5/Ch11）**。

---

## 2. 遇到了什么问题 / 当前阻碍？

1. **真实数据尚未到手**（最大阻碍）：Bristol 3-DMA `.inp` + 10 监测点数据未交接 → T2 切换真实模型、T3 数据管线都卡在这；目前用 Net3 练手顶上。
2. **`k_b` 是否跨 DMA 共享未定**（新）：3 个 DMA 若同一水源 → 倾向共享 `k_b` → 参数从 6 个降到 4 个；影响整个校准维度。
3. **"ensemble-based" 口径未细化**（新，邮件已点方向）：GLUE / ensemble Kalman / approximate Bayesian 哪一种作主线？
4. **WP1–WP5 正式结构未知**（新）：已知 WP5 = hierarchical Bayesian，其余映射待确认，影响进度报告口径。
5. **阈值与误差模型仍占位**：`0.2 mg/L`（WHO ≥0.2、UK SI 2016/614）待锁定；DPD（±0.02 / ~15%）vs 在线传感器（~5%）的误差形式待定。
6. **identifiability 已被自己证实**（不再只是文献提示）：练习 5 的补偿沟说明单点最小二乘会误导 → 支持把不确定性放进 likelihood，而非只报 RMSE。

---

## 3. 我的建议（带方案来，供讨论）

| 议题 | 我的建议 |
| --- | --- |
| 真实数据 | 主案例 = Bristol 3-DMA；数据到手前用 Net3 调通 pipeline，到手即切换 |
| `k_b` 共享 | 同一水源 ⇒ **pooled `k_b`（4 参数）**；用 A3/A6 文献 + 烧瓶值作先验，标定阶段冻结 |
| 方法路线 | **baseline WLS → Plan A: GLUE（E6）→ Plan B: Bayesian hierarchical partial pooling（E7）**；`k_w_d ~ Normal(μ, τ²)` 跨 DMA 共享族先验 |
| 入口边界 | 用 `add_source`+pattern 把实测 inlet C(t) 注入（练习 4 已验证），**入口不参与校准** |
| 跨 DMA 可迁移性 | 在 DMA-A 取后验 → 对 B/C 做 posterior predictive check（CRPS、覆盖率） |
| 阈值 | 主图 `0.2 mg/L`（F2 WHO），附录做 `0.1/0.3` 敏感性 |
| 误差模型 | 入口=在线传感器 ~5%；下游 grab=DPD ±0.02 mg/L（~15% 相对），进 likelihood |

---

## 4. 需要导师确认的事项（今天请拍板）

> 详见 [`open_questions.md`](open_questions.md)。已被 5/25 邮件回答的不再列（真实数据可用、ensemble+Bayesian 方向、3-DMA 真实网络）。

1. **数据交接**：10 监测点数据的格式/频率/时间跨度？何时交付？（建议 ≥4 周 hourly，CSV 或 SCADA dump）
2. **`.inp` 与管材**：模型文件在哪？管材/管径/管龄信息齐全度？（若缺，用 Hallam 2002 / Maleki 2023 范围作 informative prior）
3. **`k_b` 共享**：3 个 DMA 同一 `k_b` 还是各自估？（建议 pooled，4 参数）
4. **"ensemble-based" 口径**：GLUE / EnKF / approximate Bayesian？（建议 GLUE 作 baseline → Bayesian hierarchical 进阶）
5. **WP1–WP5 结构**：正式映射是什么？（我猜 WP1=Lit+Model setup、WP2=Baseline、WP3=Uncertainty、WP4=Cross-DMA、WP5=Bayesian）
6. **阈值 + 误差模型**：是否锁 `0.2 mg/L`？误差用 DPD 还是在线传感器规格？

---

## 5. 可展示材料（会议共享屏幕）

| 材料 | 路径 |
| --- | --- |
| ⭐ 5 道练习 + 我的发现 | `learning/02_exercises.ipynb`（建议 live 演示 练习1 bulk/wall + 练习5 补偿沟） |
| 补偿沟 / 拓扑 / 拆解图 | `learning/kb_kw_compensation.png`、`net3_topology.png`、`bulk_wall_decomposition.png` |
| B1 精读笔记 | `background/notes/B/B1_Klise2017.md` |
| 文献清单 | `background/Literature/literature.md` |
| Net1 baseline demo | `src/02_demo_wntr_original.py` |

---

## 6. 导师反馈

> _会议中填写_

-

---

## 7. 下一步计划（Week 3–4：06-02 → 06-12）

- [ ] 拿到 Bristol 3-DMA `.inp` + 10 监测点数据。
- [ ] 把 `simulate_chlorine` 从 Net3 切到真实模型并跑通。
- [ ] 实现 inlet → time-varying source pattern 数据管线（复用练习 4）。
- [ ] baseline 确定性校准（WLS：`k_b, k_w_A/B/C`）。
- [ ] 续读方法论文：**B3（WNTR manual 水质/反应章节）、E6（GLUE）、E7（Gelman Ch5/Ch11）**。
- [ ] `git push` 同步本地 commits；把今日决议写回 `open_questions.md` §2。
- [ ] **M1 supervisor checkpoint（06-19）准备**：baseline 完成 + GLUE 跑通 + 进度报告。

---

## 8. Action items（会议结束后 5 分钟内更新）

| # | 行动项 | 负责人 | 截止 | 状态 |
| --- | --- | --- | --- | --- |
| A1 | 记录导师对 6 项决策的结论 → `open_questions.md` §2 | 我 | 2026-06-02 | open |
| A2 | 确认 `.inp` + 数据交接方式与时间 | 我 / 导师 | 2026-06-06 | open |
| A3 | `git push` 同步本地 commits 到 GitHub | 我 | 2026-06-03 | open |
| A4 | 数据到手后启动 T3 inlet→pattern 管线 | 我 | 2026-06-12 | open |

---

## 附录：研究模块进度一览

| 模块 | 内容 | 当前状态 |
| --- | --- | --- |
| T1 文献 | 清单 + 笔记 | 🟢 进行中（A1–A6/B1 已读；待 B3/E6/E7） |
| T2 模型 | Net3 练手 → Bristol | 🟢 Net3 完成；待真实 `.inp` |
| T3 数据 | inlet→pattern + 误差模型 | 🟡 机制已验证（练习4），待真实数据 |
| T4 确定性校准 | WLS | ⚪ Week 6–7 |
| T5 不确定性 | GLUE / Bayesian hierarchical | ⚪ Week 8–9 |
| T6 跨 DMA + 解释 | 后验预测检查、阈值概率图 | ⚪ |
| T7 论文写作 | Introduction 已起草 | 🟡 Intro 草稿就绪 |
